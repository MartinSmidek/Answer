# spragma library
var the_order: text
proc show_order (o) { echo('show_order z test.elem.grp:',o); sub.show_label(o) }
group sub  {  include:'onload'
  proc show_label(o) { echo('test.elem.grp',o) }
}
use v:form g[200,0,,]
proc onstart () {
  echo(v.l.get)
}
form g [,,100,100] {
  label l [0,0,100,] { title:"nazdar" }
}
