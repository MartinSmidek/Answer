menu {type:'left',
  menu vla {title:'Účetní deník',type:'group'
    item r08 {title:'Rok 2008', par:°{rok:2008} }
    item r09 {title:'Rok 2009', par:°{rok:2009} }
  }
  proc onclick (i) {
     r.hide;
     echo(i._id,':',i.par.rok);
     { eq(i._id,'r09'); r.popup()}
  }
}
  panel r { type:'right'
    use v: form g [0,0,,]
  }
use u: form f [0,0,,]
form f {
  label result [0,0,600,] { title:'--' }
}
form g {
  label neco [0,0,600,] { title:'POZNAMKA ...' }
}
