use v: form f [200,10,,]
form f [,,100,80] {
  view w: table t
  check c [50,5,90,17] { title:'škrt'
    proc onchange() { echo('po změně=',this.get) }
  }
  button [150,5,90,] { title:"ano" proc onclick() {c.set(1)} }
  button [150,35,90,] { title:"ne" proc onclick() {c.set(0)} }
  button [150,65,90,] { title:"alert(get)" proc onclick() {alert(c.get)} }
}
